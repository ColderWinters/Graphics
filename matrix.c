struct Transformer Id_Matrix = calloc(sizeof(struct Transformer));

int i = 0;
while (i < 4) {
  Id_Matrix.colums[i].pos[i] = 1;
}

struct Transformer Multiply( *Vector mat1, *Vector mat2 ){
  int i = 0;
  while i != 0
}
